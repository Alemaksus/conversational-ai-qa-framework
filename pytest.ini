[pytest]
# Pytest configuration for Conversational AI QA Framework

# Test discovery
testpaths = tests

# Markers for test categorization
markers =
    unit: Unit tests (isolated component tests)
    matrix: Tests loaded from Excel test case matrix

# Console output options
addopts = 
    -ra
    --strict-markers
